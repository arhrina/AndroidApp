<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:paddingLeft="160px"
    android:paddingEnd="160px"
    android:paddingTop="100px"
    android:paddingBottom="300px"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!--

    리니어 레이아웃

    기본적인 레이아웃을 만들 때 사용하던 기본 구조형
    가로방향, 세로방향으로 일정하게 컴포넌트를 배치할 때 사용한다


    padding
    안드로이드 기본 디자인 가이드에 가로좌우 16px 세로위아래 8이나 10px의 여백을 주도록 추천하고 있다

    layout_width, layout_height
    wrap_content, match_parent를 주로 사용

    wrap_content는 내부에 표현되는 데이터의 크기에 따라 자동조절
    match_parent는 부모의 layout에 가득 채우도록 설정


    -->

    <!--

    텍스트뷰 : 화면에 고정된(static된) 문자열을 표현하기 위한 컴포넌트
    에디트텍스트 : 사용자로부터 키보드로 문자열 등을 입력받기 위한 컴포넌트
    버튼 : 사용자의 클릭(터치)이벤트에 반응하여 액션을 만들기 위한 컴포넌트
    -->
    <TextView
        android:id="@+id/textView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textSize="30dp"
        android:text="바보멍청이" />

    <TextView
        android:id="@+id/textView2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textSize="30dp"
        android:text="안녕" />

    <EditText
        android:id="@+id/txtInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:ems="10"
        android:inputType="textEmailAddress"
        android:selectAllOnFocus="true"
        android:text="DIE" />

    <EditText
        android:id="@+id/txtInput2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:ems="10"
        android:inputType="numberPassword"
        android:text="get away" />

    <Button
        android:id="@+id/btn1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="Toast" />

    <Button
        android:id="@+id/btn2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="Snackbar" />


    <Button
        android:id="@+id/btn3"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="Open" />

    <Button
        android:id="@+id/btn4"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="CALL" />

</LinearLayout>
